{
  "name": "movie-search-app",
  "version": "1.0.0",
  "description": "A simple web application to search for movies",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "npm run setup",
    "setup": "npm run setup:api && npm run setup:client",
    "setup:api": "cd api && npm i && cd ..",
    "setup:client": "cd client && npm i && cd .."
  },
  "author": "pia",
  "license": "Apache-2.0"
}
